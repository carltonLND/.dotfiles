# .dotfiles

1. Clone this repository
2. Run `install.sh`

## TODO

1. Add https://github.com/numToStr/Comment.nvim && https://github.com/kyazdani42/nvim-tree.lua
2. Work on nvim-cmp config 
3. fix fzf plugin not running 'make' when PlugInstall is called from nvim --headless
